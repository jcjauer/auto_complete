#include <stdlib.h>
#include <stdio.h>
#include "Autocomplete.h"

int main() {
    Node *lista = NULL;
    char palavra[MAX];

    printf("Digite uma palavra: ");
    scanf("%s\n", palavra);
    //add(&lista, palavra);
    //show(lista);

    return EXIT_SUCCESS;
}